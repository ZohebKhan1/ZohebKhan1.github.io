# Hugo.toml simplified - analytics moved to params.yaml
# baseURL and other settings in config/_default/config.yaml
# Google Analytics ID - update with your own GA4 tracking ID when available
googleAnalytics = ''
